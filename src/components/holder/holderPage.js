import React, { PropTypes } from 'react';
import toastr from 'toastr';

toastr.options.closeButton = true;

function HolderPage(props) {
  return (
    <div>null</div>
  );
}

export default HolderPage;
